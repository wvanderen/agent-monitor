~H"""
<div class="flex gap-4 mb-6">
  <.link navigate={~p"/incidents"} class="text-blue-600 hover:text-blue-800">
    ← Back to Incidents
  </.link>
</div>

<.header>
  <%= @incident.title %>
  <:subtitle>Incident details and workflow</:subtitle>
</.header>

<div class="grid gap-6 lg:grid-cols-2">
  <div class="rounded-lg bg-white shadow-sm ring-1 ring-zinc-900/5">
    <div class="border-b border-zinc-200 px-6 py-4">
      <h2 class="text-base font-semibold text-zinc-900">Incident Information</h2>
    </div>
    <div class="px-6 py-4 space-y-3">
      <div>
        <p class="text-sm font-medium text-zinc-500">Status</p>
        <span class={"inline-flex items-center rounded-md px-2 py-1 text-xs font-medium #{status_badge(@incident.status)}"}>
          <%= @incident.status %>
        </span>
      </div>
      <div>
        <p class="text-sm font-medium text-zinc-500">Severity</p>
        <span class={"inline-flex items-center rounded-md px-2 py-1 text-xs font-medium #{severity_badge(@incident.severity)}"}>
          <%= @incident.severity %>
        </span>
      </div>
      <div>
        <p class="text-sm font-medium text-zinc-500">Assigned To</p>
        <p class="text-sm text-zinc-900"><%= @incident.assigned_to || "Unassigned" %></p>
      </div>
      <div>
        <p class="text-sm font-medium text-zinc-500">Description</p>
        <p class="text-sm text-zinc-600"><%= @incident.description %></p>
      </div>
    </div>
  </div>

  <div class="rounded-lg bg-white shadow-sm ring-1 ring-zinc-900/5">
    <div class="border-b border-zinc-200 px-6 py-4">
      <h2 class="text-base font-semibold text-zinc-900">Add Comment</h2>
    </div>
    <div class="px-6 py-4">
      <form phx-submit="add_comment">
        <textarea
          name="content"
          class="w-full rounded-md border-zinc-300 text-sm"
          rows="3"
          placeholder="Add a comment..."
          phx-debounce="300"
        ><%= @comment_content %></textarea>
        <button type="submit" class="mt-2 rounded-lg bg-blue-600 px-3 py-1 text-sm font-medium text-white hover:bg-blue-700">
          Add Comment
        </button>
      </form>
    </div>
  </div>
</div>
"""
